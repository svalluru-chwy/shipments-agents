Shipments Agency Platform - Implementation Channel Log
======================================================
Created: 2026-02-12
Last Updated: 2026-02-15


Entry 1: Project Initialization and Architecture (2026-02-12)
-------------------------------------------------------------
WHAT: Created standalone shipments-agents platform from scratch.

WHY: Need a standalone shipments agency with local API capabilities,
     modeled after autoship-agency-platform. This decouples shipments
     analysis from the broader cat-agents monolith while keeping all
     17 shipment skills intact.

HOW: Analyzed 4 source repositories:
  - cat-data-operations: 28 Snowflake queries, 7 relevant to shipments
  - cat-agents: 17 shipment skills across 4 phases, skill loader infra
  - shipments-agency-streamlined-main: 7-step pipeline with validation
  - autoship-agency-platform: Template for standalone platform pattern

DECISIONS:
  1. 3-agent architecture (signals, decoder, actions) with internal
     phased skill execution (4 phases of 17 skills).
  2. Hybrid skill model: 11 deterministic Phase 1 skills for exact
     metrics + 6 LLM skills for reasoning/interpretation.
  3. No UI -- backend/API only.
  4. Exposed individual skill endpoints (/skills/{name}/run) and
     phase-level endpoints (/skills/phase/{n}/run) for debugging.
  5. Kept all conventions from autoship-agency-platform (BaseAgent,
     AgentInterface, AgentRegistry, PipelineOrchestrator, etc.).


Entry 2: Project Scaffold (2026-02-12)
---------------------------------------
WHAT: Created complete project scaffold with pyproject.toml, config.yaml,
     .env.example, .gitignore, Dockerfile, and full directory structure.

FILES:
  pyproject.toml - Core + snowflake + dev dependencies
  config.yaml - Shipments-specific agent configs (3 agents, skills config)
  .env.example - Required/optional env vars
  .gitignore - Standard Python/Node/.env patterns
  Dockerfile - Multi-stage slim build for gateway


Entry 3: Shared Infrastructure (2026-02-12)
--------------------------------------------
WHAT: Ported packages/shared/ from autoship-agency-platform with
     shipments-specific adaptations.

FILES:
  packages/shared/config/settings.py - Settings with ShipmentSignals/Decoder/Actions configs
  packages/shared/models/agent_models.py - AgentManifest, AgentRequest, AgentResponse etc.
  packages/shared/s3/client.py - Unified S3 client (identical to autoship)
  packages/shared/logging/logger.py - Stdlib logging wrapper
  packages/shared/exceptions.py - Added SkillError, ValidationError
  packages/shared/token_tracker.py - LLM cost tracking


Entry 4: Base Agent Layer (2026-02-12)
---------------------------------------
WHAT: Ported packages/agents/base/ from autoship-agency-platform.

FILES:
  packages/agents/base/agent_interface.py - Abstract interface (manifest, run, health_check)
  packages/agents/base/base_agent.py - BaseAgent with OpenAI client, S3 helpers, hooks


Entry 5: Skill Infrastructure (2026-02-12)
-------------------------------------------
WHAT: Created skill loader and phased runner adapted from cat-agents.

FILES:
  packages/agents/shipments/skills/loader.py - 3-level progressive loading
  packages/agents/shipments/skills/runner.py - Phased parallel execution
  packages/agents/shipments/skills/CONTEXT.md - Shared workflow context

KEY DESIGN: Runner supports phase_filter param so agents can run specific
     phases, and API can run individual skills or whole phases independently.


Entry 6: All 17 Shipment Skills (2026-02-12)
----------------------------------------------
WHAT: Copied all 17 shipment skills from cat-agents with their SKILL.md,
     execute.py, __init__.py, and references/data_dictionary.md files.

PHASE 1 (12 skills):
  shipment_health_check, delivery_performance, carrier_analysis,
  exception_analysis, geographic_patterns, timing_patterns,
  package_analysis, routing_efficiency, order_behavior,
  contact_correlation, current_order, shipment_signal_generator

PHASE 2 (2 skills):
  shipment_delay_predictor, shipment_signal_decoder

PHASE 3 (2 skills):
  shipment_intervention, shipment_action_prioritizer

PHASE 4 (1 skill):
  shipment_consolidator


Entry 7: Data Extraction Layer (2026-02-12)
--------------------------------------------
WHAT: Copied 7 SQL queries from cat-data-operations and ported the
     data extraction runner from autoship-agency-platform.

SQL QUERIES:
  01_main_shipment_query.sql
  02_customer_contacts_query.sql
  03_customer_zip_performance.sql
  04_benchmark_zip_performance.sql
  05_shipment_inspector_query.sql
  08_customer_information_query.sql
  15_order_shipment_summary_stats.sql

RUNNER FILES:
  packages/data_extraction/runner/data_pipeline.py
  packages/data_extraction/runner/query_registry.py
  packages/data_extraction/runner/sql_runner.py
  packages/data_extraction/run_pipeline.py (CLI entry point)


Entry 8: Three Shipment Agents (2026-02-12)
---------------------------------------------
WHAT: Created the 3 core agents that orchestrate the 17 skills.

FILES:
  packages/agents/shipments/signals/agent.py - ShipmentSignalsAgent
    - Loads shipment data from S3
    - Runs Phase 1 skills (12 skills in parallel)
    - Saves signals to S3

  packages/agents/shipments/decoder/agent.py - ShipmentDecoderAgent
    - Loads signals from upstream or S3
    - Runs Phase 2 skills (2 skills)
    - Saves decoded signals to S3

  packages/agents/shipments/actions/agent.py - ShipmentActionsAgent
    - Loads decoded signals from upstream or S3
    - Runs Phase 3 + Phase 4 skills
    - Saves consolidated output to S3


Entry 9: FastAPI Gateway (2026-02-12)
--------------------------------------
WHAT: Created FastAPI gateway adapted from autoship with shipments-specific
     routes and new skills API endpoints.

FILES:
  packages/gateway/main.py - App entry point with lifespan manager
  packages/gateway/orchestrator/pipeline.py - 3-agent pipeline orchestrator
  packages/gateway/registry/agent_registry.py - Agent registration and health
  packages/gateway/routes/health.py - /health endpoints
  packages/gateway/routes/agents.py - /agents/{name}/run etc.
  packages/gateway/routes/pipeline.py - /pipeline/run, /pipeline/batch
  packages/gateway/routes/skills.py - /skills/, /skills/{name}/run, /skills/phase/{n}/run
  packages/gateway/routes/data.py - /data/customers endpoints
  packages/gateway/routes/admin.py - /admin/refresh-health, /admin/config

NEW: Skills router exposes individual skill execution and phase-level
     execution endpoints not present in autoship.


Entry 10: Pipeline Test + Manifests (2026-02-12)
-------------------------------------------------
WHAT: Created standalone pipeline test and YAML manifests.

FILES:
  run_pipeline_test.py - Standalone test script (no gateway needed)
  skills/shipment_signals.yaml - Signals agent manifest
  skills/shipment_decoder.yaml - Decoder agent manifest
  skills/shipment_actions.yaml - Actions agent manifest
  Dockerfile - Production Docker image


Entry 11: Check Gate + Pipeline Refinement (2026-02-12)
--------------------------------------------------------
WHAT: Integrated LLM check gate into ShipmentSignalsAgent, fixed
     contact_correlation data mapping, removed Phase 3-4 from pipeline.

WHY: User clarified scope: validation agents, actions/recommendations,
     prioritization, and consolidator are NOT required. The check gate
     analysis and contact_correlation are required. Pipeline reduced to
     2 agents (signals + decoder).

HOW:
  - Ported CHECK_GATE_SYSTEM_PROMPT and helper functions from
    cat-agents/src/nodes/shipments/check_node_llm.py into signals/agent.py
  - Added _run_check_gate() with deterministic fallback when LLM fails
  - Fixed contact_correlation by mapping customer_contacts_query data to
    state["voc_data"]["contacts"] (the key the skill reads from)
  - Removed ShipmentActionsAgent from gateway and orchestrator
  - Updated run_pipeline_test.py for 2-agent pipeline with check gate output

FILES CHANGED:
  packages/agents/shipments/signals/agent.py - Check gate + contacts fix
  packages/gateway/orchestrator/pipeline.py - Removed actions agent
  packages/gateway/main.py - Removed actions agent import/registration
  run_pipeline_test.py - 2-agent pipeline, check gate display, local save


Entry 12: Bug Fixes -- current_order + shipment_delay_predictor (2026-02-13)
------------------------------------------------------------------------------
WHAT: Fixed two skill bugs that were also broken in cat-agents.

BUG 1 - current_order NoneType on .upper():
  Root cause: record.get("BULK_TRACK_LAST_STATUS_CODE_DESCRIPTION", "")
    returns None when the key exists but has a None value. .upper() crashes.
  Fix: Changed to (record.get(...) or "").upper() null-coalescing pattern.
  File: packages/agents/shipments/skills/current_order/execute.py line 70

BUG 2 - shipment_delay_predictor missing shipments_result:
  Root cause: In cat-agents, a separate shipments_analysis_node (graph node)
    creates state["shipments_result"] before skills run. Our standalone
    pipeline has no such node, so the key never exists.
  Fix: Added _find_active_shipments() fallback function that checks:
    1. state[target_result_key].active_shipments (original cat-agents path)
    2. state["current_order_result"]["grounded_metrics"]["active_order_details"]
       (Phase 1 result injected between phases)
    3. state["shipment_data"]["records"] filtered for undelivered records
  Also returns a clean "no_active_shipments" result dict instead of None
    when all shipments are delivered.
  File: packages/agents/shipments/skills/shipment_delay_predictor/execute.py

RESULT: Both skills now complete successfully. current_order found 1 active
  order (tracking D10017134276666, OnTrac, shipped 2026-02-12).
  delay_predictor analyzed the active shipment via LLM and returned route
  performance data (MCO1->85142 at 33.3% delay rate vs PHX1->85142 at 0%).
  These were both broken in cat-agents for the same customer.


Entry 13: Output Quality Comparison -- shipments-agents vs cat-agents (2026-02-13)
-----------------------------------------------------------------------------------
Compared pipeline outputs for customer 6180005.
  cat-agents run: 2026-02-11 (analysis_20260211_151005.json)
  shipments-agents runs: 2026-02-12 (run 1, pre-fix) and 2026-02-13 (run 2, post-fix)

Note: slight metric differences (CTD, carrier %) are expected because
  the data windows differ by 1-2 days and the underlying S3 data may have
  received new shipment records between runs.

--- PHASE 1: DETERMINISTIC SKILLS (10 skills) ---

SKILL                    | cat-agents              | shipments-agents       | VERDICT
-------------------------|-------------------------|------------------------|--------
shipment_health_check    | HEALTHY, CTD 2.79,      | HEALTHY, CTD 2.72,     | MATCH
                         | 50th pctile, 100% OT    | 40th pctile, 100% OT   |
delivery_performance     | ATTENTION, CTD 2.79,    | ATTENTION, CTD 2.72,   | MATCH
                         | 13.3% breach, STABLE    | 13.3% breach, DECLINING|
carrier_analysis         | FSMS 63.3%, CTD 3.17    | FSMS 60.0%, CTD 3.06   | MATCH
                         | OnTrac 33.3%, CTD 2.0   | OnTrac 36.7%, CTD 2.0  |
exception_analysis       | 0 exceptions (0%)       | 0 exceptions (0%)      | MATCH
geographic_patterns      | RNO1 53.3%, CTD 1.87    | RNO1 56.7%, CTD 1.81   | MATCH
timing_patterns          | Thu best, Fri worst     | Thu best, Fri worst    | MATCH
                         | Fri CTD 4.0             | Fri CTD 4.25           |
package_analysis         | avg 5.44 lbs, 0% heavy  | avg 7.66 lbs, 3.3% hvy| DIFF (*)
                         | range 0.4-11.7          | range 0.4-53.9         |
routing_efficiency       | NO_DATA, 0% efficiency  | 76.3% efficiency,      | IMPROVED
                         |                         | 193 excess miles, RNO1 |
order_behavior           | 86.7% autoship, 10.5/mo | 86.7% autoship, 10.6/mo| MATCH
contact_correlation      | 20% rate, 0% WISMO      | 20% rate, 0% WISMO     | MATCH

(*) package_analysis diff: shipments-agents includes a 53.9 lb shipment
    that was not in the cat-agents data window. This is a data freshness
    difference, not a skill logic difference.

ROUTING_EFFICIENCY improvement: shipments-agents has actual arc distance
  data from shipment_inspector_query (816 avg miles, 623 optimal). cat-agents
  returned NO_DATA. This means the shipment_inspector_query in S3 was either
  missing or not loaded in the cat-agents run.

--- PHASE 1: FIXED SKILLS (2 skills) ---

SKILL                    | cat-agents              | shipments-agents       | VERDICT
-------------------------|-------------------------|------------------------|--------
current_order            | CRASHED: NoneType error  | 1 active order found,  | FIXED
                         |                         | 0 at-risk, VOC checked |
(new) CHECK GATE         | Not present             | LLM GREEN, severity    | NEW
                         |                         | LOW, 2/22 delayed,     |
                         |                         | 9.1% rate, no at-risk  |

--- PHASE 1: LLM SKILLS (1 skill) ---

SKILL                    | cat-agents              | shipments-agents       | VERDICT
-------------------------|-------------------------|------------------------|--------
signal_generator         | 3 signals, all Normal   | Run 1: 3 signals incl  | IMPROVED
                         | Processing, all low     | 1 HIGH "Excessive      | (run 1)
                         | severity, none          | Delay" CTD 5.44 days,  |
                         | actionable              | 1 actionable           |
                         |                         | Run 2: 0 signals (LLM  | VARIANCE
                         |                         | non-determinism)       | (run 2)

Signal generator run 1 was qualitatively better: it correctly identified
  a HIGH severity Excessive Delay (order 5098639659, CTD 5.44 days exceeding
  the 4.23 threshold) that cat-agents missed. cat-agents classified all 3
  signals as "Normal Processing" despite CTD values of 1, 2, and 2 days.

--- PHASE 2: LLM SKILLS (2 skills) ---

SKILL                    | cat-agents              | shipments-agents       | VERDICT
-------------------------|-------------------------|------------------------|--------
signal_decoder           | 3 signals decoded,      | Run 1: 3 signals       | MATCH/
                         | all P4 / no action      | decoded with proper    | IMPROVED
                         |                         | severity, route info   | (run 1)
                         |                         | Run 2: no signals      | N/A
                         |                         | to decode              | (run 2)
shipment_delay_predictor | CRASHED: No result      | LLM analysis of 1     | FIXED
                         | returned                | active shipment,       |
                         |                         | route performance      |
                         |                         | (MCO1 33.3% delay      |
                         |                         | rate, PHX1 0%)         |

--- PHASE 3-4: EXCLUDED BY DESIGN ---

The following skills run in cat-agents but are intentionally excluded
from the shipments-agents pipeline per user requirement:
  - shipment_intervention (Phase 3): 0 interventions needed for 6180005
  - shipment_action_prioritizer (Phase 3): 3 actions (2 P1, 1 P2) generated
  - shipment_consolidator (Phase 4): ATTENTION overall, 11 skills consolidated

These skills and their agent (ShipmentActionsAgent) still exist in the
codebase but are not wired into the pipeline.

--- OVERALL ASSESSMENT ---

Of 14 comparable skill outputs:
  - 9 MATCH (deterministic skills produce equivalent results)
  - 2 FIXED (current_order, delay_predictor -- both broken in cat-agents)
  - 1 IMPROVED (routing_efficiency has actual data vs NO_DATA)
  - 1 NEW (check gate LLM analysis, not present in cat-agents)
  - 1 DIFF (package_analysis, data freshness only)

Signal quality: shipments-agents run 1 produced better signal detection
  (caught a HIGH severity delay cat-agents missed). Run 2 showed LLM
  variance with 0 signals -- inherent to non-deterministic LLM calls.

Conclusion: shipments-agents produces output at parity or better than
  cat-agents for all shared skills, with two previously broken skills
  now working and a new check gate capability added.


Entry 14: Skill Data Fallback Fixes (2026-02-13)
-------------------------------------------------
WHAT: Added data fallback chains across 7 skills to handle missing/null
     fields in S3 data. These fix silent misclassifications caused by
     SHIPMENT_WAS_DELAYED being None for all records, BULK_TRACK_DELIVERY_DTTM
     being None for delivered shipments, and weight field gaps.

WHY: Validation against raw S3 data for customer 6180005 revealed that
     skills were producing incorrect results due to data quality issues:
     - All 30 records had SHIPMENT_WAS_DELAYED=None, making delay detection
       fail silently (0% delay rate when actual was ~37%).
     - Shipment 500776879286 had BULK_TRACK_DELIVERY_DTTM=None but
       SHIPMENT_STATUS=DELIVERED, causing false "active" classification.
     - delivery_performance claimed "all delivered" while 2 records
       had no CTD (undelivered/in-transit).
     - exception_analysis type categorization showed "No Exception" for
       records flagged only via ONTIME_DELIVERY_FLAG=0.

HOW: Implemented fallback chains in each skill:

  1. current_order (execute.py):
     - Status: BULK_TRACK_LAST_STATUS_CODE_DESCRIPTION -> SHIPMENT_STATUS
       -> WIZMO_CURRENT_PKG_STATUS
     - Delivered check: BULK_TRACK_DELIVERY_DTTM not None -> SHIPMENT_STATUS
       == "DELIVERED" -> WIZMO_CURRENT_PKG_STATUS == "DELIVERED"
     - Expected delivery: EXPECTED_DELIVERY_DATE -> SHIPMENT_ESTIMATED_DELIVERY_DATE
       -> WIZMO_CURRENT_ARRIVAL_DATE -> LAST_EXPECTED_DELIVERY_DATE
     - Added _is_delivered() helper for consistent delivered count

  2. shipment_health_check (execute.py):
     - Delayed: SHIPMENT_WAS_DELAYED=True -> ONTIME_DELIVERY_FLAG=0
     - Delivered classification: BULK_TRACK_DELIVERY_DTTM -> SHIPMENT_STATUS
       -> WIZMO_CURRENT_PKG_STATUS (reports delivered/undelivered counts)
     - Updated delay_definition metadata

  3. package_analysis (execute.py):
     - Weight: BULK_TRACK_LB_PACKAGE_WEIGHT -> SHIPMENT_PLANNED_WEIGHT
       -> WAREHOUSE_SCALE_WEIGHT

  4. delivery_performance (execute.py):
     - Delayed: CTD > threshold -> SHIPMENT_WAS_DELAYED=True
       -> ONTIME_DELIVERY_FLAG=0
     - Reports delivered_with_ctd vs undelivered_count explicitly
     - No longer claims "all delivered" when undelivered records exist
     - Updated delay_definition metadata

  5. carrier_analysis (execute.py):
     - Delayed per carrier: same 3-way fallback as delivery_performance

  6. shipment_delay_predictor (execute.py):
     - _find_active_shipments() Source 3: added SHIPMENT_STATUS and
       WIZMO_CURRENT_PKG_STATUS fallbacks for delivered detection and
       status resolution

  7. exception_analysis (execute.py):
     - Added ONTIME_DELIVERY_FLAG=0 as exception signal
     - Added SHIPMENT_WAS_LOST=True check
     - Fixed exception type categorization: only uses exc_desc when
       has_exception is True; otherwise assigns descriptive type
       (Lost/Delayed/Not on-time)
     - Reports lost_shipment_count in grounded_metrics

RESULT (pipeline run for 6180005):
  - current_order: 29 delivered, 1 active (was 28 delivered, 2 active before)
    Shipment 500776879286 now correctly classified via SHIPMENT_STATUS fallback
  - health_check: 11 delayed (36.7%) via ONTIME_DELIVERY_FLAG (was 0%)
    Reports 29 delivered / 1 undelivered
  - delivery_performance: 14 delayed (46.7%) via combined fallback chain
    Reports 28 with CTD / 2 without, explicitly notes undelivered
  - carrier_analysis: FedEx 72.2% delayed, OnTrac 9.1% delayed
  - exception_analysis: 11 exceptions (36.7%) categorized by type
  - delay_predictor: 1 active shipment correctly identified (5101134637)
  - package_analysis: weight range 0.4-54.4 lbs using fallback chain

FILES CHANGED:
  packages/agents/shipments/skills/current_order/execute.py
  packages/agents/shipments/skills/shipment_health_check/execute.py
  packages/agents/shipments/skills/package_analysis/execute.py
  packages/agents/shipments/skills/delivery_performance/execute.py
  packages/agents/shipments/skills/carrier_analysis/execute.py
  packages/agents/shipments/skills/shipment_delay_predictor/execute.py
  packages/agents/shipments/skills/exception_analysis/execute.py


Entry 15: CTD-Only Delay Detection (2026-02-13)
------------------------------------------------
WHAT: Removed ONTIME_DELIVERY_FLAG from all delay/exception detection.
     Delay is now purely CTD > threshold. Added estimated CTD fallback
     for records missing CLICK_TO_DELIVER_DAYS.

WHY: Validation against raw S3 data for customer 6180005 showed that
     ONTIME_DELIVERY_FLAG does not correlate with actual delivery timeliness.
     All 11 records with ONTIME_DELIVERY_FLAG=0 delivered ON or BEFORE
     their expected delivery date. The flag appears to measure something
     other than delivery vs estimated date (possibly an internal SLA we
     don't have visibility into). Using it inflated delay counts from 4
     to 11-14 with false positives.

HOW:
  1. shipment_health_check: Removed ONTIME_DELIVERY_FLAG fallback.
     Added _parse_date() helper. Added estimated CTD computation from
     SHIPMENT_ESTIMATED_DELIVERY_DATE / WIZMO_CURRENT_ARRIVAL_DATE /
     LAST_EXPECTED_DELIVERY_DATE - ORDER_PLACED_DTTM. Computes its own
     CTD threshold (mean + 1 std dev) for delay detection. Tracks
     estimated_ctd_count in grounded_metrics.

  2. delivery_performance: Removed ONTIME_DELIVERY_FLAG from delayed
     detection. Added _parse_date() + estimated CTD fallback. Now tracks
     actual_ctd_count, estimated_ctd_count, no_ctd_count. Flagged
     shipments include ctd_source ("actual" or "estimated").

  3. carrier_analysis: Removed ONTIME_DELIVERY_FLAG. Added _parse_date()
     + estimated CTD fallback. Delayed per carrier = CTD > threshold only.

  4. exception_analysis: Removed is_not_ontime and "Not on-time
     (ONTIME_DELIVERY_FLAG=0)" type. Exceptions now = actual carrier
     exceptions + SHIPMENT_WAS_DELAYED + SHIPMENT_WAS_LOST only.

RESULT (pipeline run for 6180005):
  - health_check: 4 delayed (13.3%), threshold 4.19 (incl 2 estimated CTDs)
  - delivery_performance: 4 delayed (13.3%), threshold 4.29
    Flagged: 494399793244 (CTD=5), 496272761367 (CTD=5),
             D10016935065020 (CTD=5), 476834403040 (CTD=7)
  - carrier_analysis: FedEx 3 delayed (16.7%), OnTrac 1 delayed (9.1%)
  - exception_analysis: 0 exceptions (0%)
  - 2 estimated CTDs computed (500776879286: ~1d, D10017134276666: ~2d)
  - All 30 records now have CTD coverage (28 actual + 2 estimated)

FILES CHANGED:
  packages/agents/shipments/skills/shipment_health_check/execute.py
  packages/agents/shipments/skills/delivery_performance/execute.py
  packages/agents/shipments/skills/carrier_analysis/execute.py
  packages/agents/shipments/skills/exception_analysis/execute.py

================================================================================
ENTRY 16 - Config Cleanup: Remove Dead o3 Model References
DATE: 2026-02-13
================================================================================

WHAT: Replaced all o3-2025-04-16 config defaults with gpt-5-nano-2025-08-07
      to match actual runtime behavior. Removed stale o3 pricing entry.

WHY: The o3-2025-04-16 model was configured in 3 fields (contacts_model,
     decoder model, decoder_model) across settings.py and config.yaml, but
     none of these values were read by any skill execution code. All 6 LLM
     skills use os.getenv("OPENAI_MODEL", "gpt-5-nano-2025-08-07") directly,
     so gpt-5-nano was always the actual model running. The config was
     misleading and needed to reflect reality.

HOW:
  1. settings.py: Changed 3 defaults from "o3-2025-04-16" to
     "gpt-5-nano-2025-08-07":
     - ShipmentSignalsAgentConfig.contacts_model
     - ShipmentDecoderAgentConfig.model
     - ShipmentDecoderAgentConfig.decoder_model

  2. config.yaml: Same 3 fields updated to "gpt-5-nano-2025-08-07".

  3. token_tracker.py: Removed the o3-2025-04-16 pricing entry (gpt-5-nano
     pricing already existed and is the only model in use).

RUNTIME IMPACT: None. These config values are not read by any execution
code; this is purely a config cleanup.

FILES CHANGED:
  packages/shared/config/settings.py
  config.yaml
  packages/shared/token_tracker.py


================================================================================
ENTRY 17 - Signal Generator Rewrite: Pre-computed Anomalies + Record Trimming
DATE: 2026-02-15
================================================================================

WHAT: Rewrote the shipment signal generator with a deterministic Python
      anomaly pre-filter, shared record trimming utility, and updated
      output scope (signals/observations + analysis + intervention-needed,
      no actions or recommendations). Applied record trimming to 3 other
      skills that send raw records to LLM.

WHY: The signal generator was unreliable -- alternating between 0 and 30
     signals across runs due to three root causes:
     1. PROMPT BLOAT: Each record had 274 fields (~12,500 chars JSON).
        Sending 50 records produced ~102K token prompts, overwhelming
        the model and causing hallucinated tracking numbers (TRK_001).
     2. CONFLICTING INSTRUCTIONS: SKILL.md mandated "signal for EVERY
        order" while the system prompt said "naturally run out of patterns".
        These directly contradicted, causing non-deterministic behaviour
        (0 or 30 signals depending on which instruction the LLM followed).
     3. ORDER_ID MISMATCH: Prompts referenced ORDER_ID (21% populated)
        instead of ORDERS_ORDER_ID (100% populated), so the LLM couldn't
        find real order IDs and fabricated them.

HOW:
  1. Created packages/agents/shipments/skills/record_trimmer.py:
     - Shared utility with trim_record() and trim_records() functions
     - Reduces 274 fields to 26 relevant fields (~90% reduction)
     - ONTIME_DELIVERY_FLAG intentionally excluded (unreliable, Entry 15)
     - Fields: ORDERS_ORDER_ID, SHIPMENT_TRACKING_NUMBER, CUSTOMER_ID,
       ORDER_PLACED_DTTM, ACTUAL_SHIP_DATE, BULK_TRACK_DELIVERY_DTTM,
       BULK_TRACK_ESTIMATED_DELIVERY_DTTM, SHIPMENT_ESTIMATED_DELIVERY_DATE,
       CLICK_TO_DELIVER_DAYS, SHIP_TO_DELIVER_DAYS, SHIPMENT_WAS_DELAYED,
       SHIPMENT_STATUS, BULK_TRACK_LAST_STATUS_CODE_DESCRIPTION,
       WAREHOUSE_CARRIER, FFMCENTER_NAME, ACTUAL_SHIP_ROUTE, POSTCODE,
       ACTUAL_ZONE, LINEITEM_PRODUCT_NAMES, LINEITEM_PRODUCT_CAT_L1,
       LINEITEM_PRODUCT_MC2, SHIPMENT_CONTAINS_FRESH,
       ORDERS_ORDER_AUTO_REORDER_FLAG, BULK_TRACK_LB_PACKAGE_WEIGHT,
       SHIPMENT_COUNT_OF_ITEMS_IN_BOX, BULK_TRACK_DELIVERY_ATTEMPT_EXCEPTION

  2. Rewrote shipment_signal_generator/execute.py:
     - Added _flag_anomalies() deterministic Python function that splits
       records into anomalous vs normal based on:
       * CTD exceeds baseline threshold (ctd_avg + 1 std dev)
       * SHIPMENT_WAS_DELAYED is truthy
       * Active/in-transit (no delivery date, status != DELIVERED)
       * Carrier exception present (excludes "No Exception" false positives)
       * Fresh/Rx items with elevated CTD
     - Each flagged record gets a _flags list explaining why it was flagged
     - Builds compact normal-shipment summary (count, avg CTD, carriers)
     - Sends only flagged (trimmed) records + normal summary to LLM
     - If no anomalies detected, returns clean result without LLM call
     - Updated system prompt: signals/observations + analysis +
       intervention-needed only. NO actions, NO recommendations
     - References ORDERS_ORDER_ID (not ORDER_ID) throughout

  3. Rewrote shipment_signal_generator/SKILL.md:
     - Removed "signal for EVERY order" mandate
     - New mandate: "one signal per flagged shipment"
     - Added baseline summary requirement
     - Removed actions/recommendations from output scope
     - Added explicit "Do NOT" section for ORDER_ID usage
     - Updated output format to match new schema

  4. Applied record trimming to 3 other skills:
     - shipment_signal_decoder/execute.py: trim_records() on 10 sample records
     - shipment_delay_predictor/execute.py: trim_record() on 1 active shipment
     - shipment_intervention/execute.py: trim_record() on 1 shipment

RESULT (2 pipeline runs for customer 6180005):

  Run 1 (pre-exception-fix):
    - 20 records flagged (4 CTD breaches + 16 "No Exception" false positives)
    - 10 signals in markdown output, all with real IDs
    - 3 HIGH severity (CTD breaches), 7 LOW (false carrier exceptions)
    - Duration: 301.2s total (211.2s signals + 90.0s decoder)

  Run 2 (post-exception-fix):
    - 4 records flagged (all genuine CTD breaches)
    - 4 signals generated, ALL with real ORDERS_ORDER_IDs:
      * 5094661531 / 494399793244 - CTD 5, Apoquel dog medication
      * 5077007697 / 496272761367 - CTD 5, horse supplement
      * 5062954686 / D10016935065020 - CTD 5, Apoquel dog medication
      * 5060501565 / 476834403040 - CTD 7, Apoquel dog medication
    - All 4 signals correctly flagged as intervention_needed=true
    - Continued analysis paragraph referenced all 4 order IDs with metrics
    - Normal summary: "26 shipments delivered normally, avg CTD 2.2 days"
    - Duration: 217.4s total (133.6s signals + 83.8s decoder)

  Comparison with previous runs:
    - Before rewrite: 0 signals (LLM variance) or 30 signals (noise)
    - After rewrite: 4 signals deterministically (Python catches anomalies)
    - Runtime improved: 217s vs 301s (28% faster due to smaller prompts)
    - Signal quality: all real IDs, all real metrics, no hallucination
    - Decoder properly decoded all 4 signals with routing analysis

FILES CREATED:
  packages/agents/shipments/skills/record_trimmer.py

FILES CHANGED:
  packages/agents/shipments/skills/shipment_signal_generator/execute.py
  packages/agents/shipments/skills/shipment_signal_generator/SKILL.md
  packages/agents/shipments/skills/shipment_signal_decoder/execute.py
  packages/agents/shipments/skills/shipment_delay_predictor/execute.py
  packages/agents/shipments/skills/shipment_intervention/execute.py
  docs/channel_log.txt


================================================================================
ENTRY 18 - Fix delay predictor: trust current_order for active shipment detection
DATE: 2026-02-15
================================================================================

WHAT: Fixed the delay predictor's _find_active_shipments() function so it
      trusts the current_order skill's determination instead of falling through
      to a raw-record filter that disagreed.

WHY: The delay predictor was finding 1 "active" shipment (D10017134276666)
     while current_order, shipment_health_check, and all other Phase 1 skills
     agreed all 30 shipments were delivered. The root cause was a fallback
     chain bug:
       - Source 2 checked current_order_result.active_order_details
       - current_order correctly found 0 active, so the list was []
       - `if details:` evaluated to False on the empty list
       - This fell through to Source 3 (raw record filter) which used
         a subtly different delivered-detection logic
       - Source 3 found D10017134276666 as "active" because its
         BULK_TRACK_DELIVERY_DTTM was None and BULK_TRACK_LAST_STATUS_CODE_DESCRIPTION
         was not "DELIVERED", even though SHIPMENT_STATUS was "DELIVERED"
     The result was a misleading "INDETERMINATE" delay prediction for a
     shipment that was already delivered per the authoritative source.

HOW:
  1. Reordered source priority: current_order_result is now Source 1
     (was Source 2). When it exists (Phase 1 ran), its determination
     is authoritative -- return its active list even if empty.

  2. Extracted _is_delivered() helper that replicates current_order's
     exact delivered-detection logic:
       - BULK_TRACK_DELIVERY_DTTM is not None (primary)
       - SHIPMENT_STATUS == "DELIVERED" (fallback)
       - WIZMO_CURRENT_PKG_STATUS == "DELIVERED" (fallback)

  3. Source 3 (standalone fallback) now uses _is_delivered() for
     consistency with current_order when running without Phase 1.

  4. Legacy cat-agents shipments_result path kept as Source 2 for
     backwards compatibility.

RESULT (pipeline run for 6180005):
  - delay_predictor: "No active or in-transit shipments found.
    All shipments appear to have been delivered." -- matches current_order
  - No LLM call made (returned clean result instantly)
  - signal_decoder: 4 signals decoded correctly (unaffected)
  - Runtime: 171.4s total (98.8s signals + 72.6s decoder)
    Previous run: 217.4s -- 21% faster (saved ~18s LLM call + ~28s
    from general variance in check gate/signal generator response times)

FILES CHANGED:
  packages/agents/shipments/skills/shipment_delay_predictor/execute.py
  docs/channel_log.txt


================================================================================
ENTRY 19 - Intervention Assessment: Phase 3 synthesis skill
DATE: 2026-02-15
================================================================================

WHAT: Rewrote the shipment_intervention skill from an action-recommender
      into an intervention assessment that synthesises all Phase 1+2 results
      into a customer-level determination. Wired Phase 3 into the decoder agent.

WHY: The original intervention skill generated specific actions (notification
     drafts, carrier escalation steps, SLA timelines) which violated the
     "no actions, no recommendations" requirement. It also only looked at 1
     shipment from the delay predictor, used hallucinated success rates, and
     depended on the stale shipments_result key. The pipeline needed a
     synthesis step that answers: "Is intervention warranted? At what urgency?
     Why?" -- referencing all upstream results.

HOW:
  1. Rewrote shipment_intervention/execute.py:
     - Gathers ALL upstream results: signal_generator (signals), signal_decoder
       (decoded analysis), delay_predictor, and Phase 1 metrics (health_check,
       delivery_performance, carrier_analysis, contact_correlation, current_order,
       check_gate)
     - Deterministic _compute_assessment() computes:
       * intervention_warranted (bool)
       * urgency (critical/high/medium/low/none) based on signal counts:
         3+ high -> critical, any high -> high, any medium -> medium, etc.
       * rationale bullets (signal counts, trend direction, contact rate, WISMO)
       * signal summaries with real IDs
       * contributing factors from Phase 1 metrics
     - Lightweight LLM call (_write_synthesis) writes one synthesis paragraph
       from the deterministic assessment -- focused on intervention context
     - Falls back to deterministic summary if LLM fails
     - NO actions, NO recommendations, NO SLA targets, NO communication drafts

  2. Rewrote shipment_intervention/SKILL.md:
     - Changed from action-recommender to intervention-assessment
     - Output scope: warranted determination, urgency, rationale, pet care impact
     - Explicit "Do NOT include actions, recommendations, next steps"

  3. Updated runner.py Phase 3:
     - Removed shipment_action_prioritizer (generates actions, not wanted)
     - Phase 3 now contains only shipment_intervention

  4. Updated decoder/agent.py:
     - Changed phase_filter from [2] to [2, 3]
     - Phase 2 (delay_predictor + signal_decoder) runs first in parallel
     - Phase 3 (intervention assessment) runs after, seeing all results
     - Updated markdown builder to include continued_analysis paragraph

RESULT (pipeline run for 6180005):
  - Phase 3 ran successfully after Phase 2
  - Assessment: intervention_warranted=true, urgency=critical
  - Rationale: "4 of 4 signals flagged as intervention-needed (0 critical,
    4 high, 0 medium). Delivery performance trend is DECLINING."
  - Synthesis paragraph referenced all 4 order IDs (5094661531, 5077007697,
    5062954686, 5060501565), carrier patterns (FedEx FSMS PHX1/PHX2, OnTrac
    WVAN_TO), CTD metrics, pet care impact (Apoquel Rx medication, horse
    supplements), contributing factors (avg CTD 2.67, 86.7% on-time,
    contact rate 20.0, DECLINING trend)
  - LLM synthesis call took ~19 seconds
  - Total pipeline: 259.6s (163.8s signals + 95.8s decoder with Phase 2+3)

FILES CHANGED:
  packages/agents/shipments/skills/shipment_intervention/execute.py
  packages/agents/shipments/skills/shipment_intervention/SKILL.md
  packages/agents/shipments/skills/runner.py
  packages/agents/shipments/decoder/agent.py
  docs/channel_log.txt

================================================================================
ENTRY 20 - Replace Phase 3 LLM intervention with recency-aware signals +
           deterministic customer risk profile
================================================================================
DATE: 2026-02-15

WHY:
  Phase 3 (shipment_intervention) was adding limited value:
  - Mostly rehashed Phase 1+2 data without new insight
  - Incorrectly labelled all 4 historical delays as "critical urgency"
  - Could not distinguish a 72-day-old delivered event from an active one
  - "Contributing factors" were basic stats, not causal patterns
  - Required an LLM call (~19s) for a synthesis paragraph

  Meanwhile, the signal generator marked all signals as intervention_needed=true
  regardless of whether the event was 11 days or 72 days old.

  The actual value lies in two things the pipeline was NOT doing:
  1. Recency-aware intervention determination (recent vs historical vs active)
  2. Cross-skill pattern analysis (carrier+route correlation, forward risk)

WHAT WAS IMPLEMENTED:

  Part A - Recency-aware signal generator:

  1. _flag_anomalies now computes _recency and _days_since_event for each
     flagged record before it goes to the LLM:
     - "active": not yet delivered (days_since_event = 0)
     - "recent": delivered within 14 days
     - "historical": delivered >14 days ago
     Uses BULK_TRACK_DELIVERY_DTTM (primary) or ORDER_PLACED_DTTM (fallback).

  2. SYSTEM_PROMPT updated: intervention_needed=true only for active/recent
     events, false for historical (they are pattern context, not actionable).
     Output schema now includes recency and days_since_event fields.

  3. SKILL.md updated to document recency fields and intervention logic.

  Part B - Deterministic customer risk profile skill:

  New skill: customer_risk_profile (packages/agents/shipments/skills/
  customer_risk_profile/). Pure Python, zero LLM calls. Reads all Phase 1+2
  results from state and computes 4 risk dimensions:

  1. Temporal Recency: Classifies signals into active/recent/historical
     buckets. E.g., "1 of 4 signals is recent, 3 are historical."

  2. Pattern Correlation: Groups signals by carrier + FC route. Finds
     dominant pattern. Cross-references with carrier delay rates from
     carrier_analysis_result. E.g., "3 of 4 delays share FedEx FSMS via
     PHX1/PHX2. Carrier delay rate: 16.7%."

  3. Forward Risk: Estimates future delay exposure from autoship_rate x
     primary_carrier_share x carrier_delay_rate. Factors in trend direction.
     E.g., "At 10.6 orders/month, expect ~1.1 delayed orders/month."

  4. Relationship Signal: Combines WISMO rate + contact rate + Rx product
     detection + active-at-risk count. E.g., "0% WISMO despite 4 delays.
     Rx medication on record."

  Overall risk level computed from dimensions:
  - high: active signals, or recent + Rx
  - elevated: carrier pattern + declining trend
  - moderate: signals but all historical
  - low: no signals or improving trend

  Part C - Pipeline wiring:

  1. runner.py: Phase 3 now contains customer_risk_profile instead of
     shipment_intervention.
  2. decoder/agent.py: Keeps phase_filter=[2, 3]. Updated description and
     markdown builder to render risk profile dimensions.
  3. shipment_intervention/ stays dormant in the repo.

HOW IT WAS IMPLEMENTED:
  - packages/agents/shipments/skills/shipment_signal_generator/execute.py:
    Added dateutil import, _parse_datetime helper, RECENCY_WINDOW_DAYS
    constant. Modified _flag_anomalies to compute _recency and
    _days_since_event. Updated SYSTEM_PROMPT with recency-aware intervention
    logic and expanded output schema.

  - packages/agents/shipments/skills/shipment_signal_generator/SKILL.md:
    Added _recency and _days_since_event to input schema. Updated
    intervention_needed description and output example.

  - packages/agents/shipments/skills/customer_risk_profile/__init__.py:
    Empty init.

  - packages/agents/shipments/skills/customer_risk_profile/execute.py:
    Four dimension functions (_compute_temporal_recency,
    _compute_pattern_correlation, _compute_forward_risk,
    _compute_relationship_signal) plus _compute_overall_risk. Main execute()
    reads state and returns structured output.

  - packages/agents/shipments/skills/customer_risk_profile/SKILL.md:
    Documents the skill, its inputs, 4 dimensions, and output scope.

  - packages/agents/shipments/skills/runner.py:
    Phase 3 changed from shipment_intervention to customer_risk_profile.

  - packages/agents/shipments/decoder/agent.py:
    Updated description and comments. Added _render_risk_profile method
    for markdown output.

FILES CHANGED:
  packages/agents/shipments/skills/shipment_signal_generator/execute.py
  packages/agents/shipments/skills/shipment_signal_generator/SKILL.md
  packages/agents/shipments/skills/customer_risk_profile/__init__.py (new)
  packages/agents/shipments/skills/customer_risk_profile/execute.py (new)
  packages/agents/shipments/skills/customer_risk_profile/SKILL.md (new)
  packages/agents/shipments/skills/runner.py
  packages/agents/shipments/decoder/agent.py
  docs/channel_log.txt
